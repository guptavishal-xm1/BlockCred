spring:
  datasource:
    url: jdbc:h2:mem:blockcred;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  h2:
    console:
      enabled: true
  cache:
    cache-names: verificationByHash
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

blockcred:
  scheduling:
    enabled: true
  worker:
    cooldown-seconds: 60
    due-batch-size: 100
    max-batches-per-tick: 5
    stale-sent-seconds: 120
  verify:
    chain-lookup-timeout-ms: 800
  ops:
    pending-max-age-minutes: 10
    retry-threshold: 3
    revocation-grace-minutes: 15
    consistency-scan-interval-minutes: 60
    consistency-scan-window-hours: 24
  auth:
    admin-token: blockcred-admin-dev-token-change-me
    legacy-header-enabled: true
    issuer-token-enabled: false
    issuer-token: blockcred-issuer-dev-token-change-me
    jwt:
      issuer: blockcred-staff
      active-kid: v1
      active-key: blockcred-jwt-dev-active-key-change-me
      previous-kid:
      previous-key:
      access-token-ttl-minutes: 15
    refresh-token-ttl-days: 7
    login:
      lock-threshold: 5
      lock-minutes: 15
    seed:
      enabled: true
      admin-username: admin
      admin-email: admin@blockcred.local
      admin-display-name: University Admin
      admin-password: AdminPass#2026
      issuer-username: issuer
      issuer-email: issuer@blockcred.local
      issuer-display-name: University Issuer
      issuer-password: IssuerPass#2026
  wallet:
    enabled: true
    disabled-retry-seconds: 60
    key-source: ENV
    key-file-path:
  public:
    token-secret: blockcred-dev-secret-change-me
    token-issuer: blockcred
    token-ttl-days: 180
    verifier-base-url: http://localhost:5173
    tx-explorer-base-url: https://amoy.polygonscan.com/tx
  audit:
    digest-secret: blockcred-audit-dev-secret-change-me
    digest-cron: "0 15 0 * * *"
    retention-days: 365
    digest-retention-days: 400
    retention-cron: "0 30 1 * * *"

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info
  endpoint:
    health:
      show-details: always
